{
  "name": "zerokey-ai",
  "version": "1.0.0",
  "private": true,
  "description": "ZeroKey AI — static local-first multi-AI orchestrator with encrypted local key vault.",
  "scripts": {
    "check": "node tests/static/check-html.js",
    "test": "npm run check",
    "test:e2e": "playwright test",
    "serve": "http-server . -p 4173 -c-1"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "http-server": "^14.1.1"
  }
}
